package typingsJapgolly.phaser.global

import typingsJapgolly.phaser.Phaser.Scene
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

@JSGlobal("SpineGameObject")
@js.native
open class SpineGameObject protected ()
  extends StObject
     with typingsJapgolly.phaser.SpineGameObject {
  def this(scene: Scene, pluginManager: typingsJapgolly.phaser.SpinePlugin, x: Double, y: Double) = this()
  def this(scene: Scene, pluginManager: typingsJapgolly.phaser.SpinePlugin, x: Double, y: Double, key: String) = this()
  def this(
    scene: Scene,
    pluginManager: typingsJapgolly.phaser.SpinePlugin,
    x: Double,
    y: Double,
    key: String,
    animationName: String
  ) = this()
  def this(
    scene: Scene,
    pluginManager: typingsJapgolly.phaser.SpinePlugin,
    x: Double,
    y: Double,
    key: Unit,
    animationName: String
  ) = this()
  def this(
    scene: Scene,
    pluginManager: typingsJapgolly.phaser.SpinePlugin,
    x: Double,
    y: Double,
    key: String,
    animationName: String,
    loop: Boolean
  ) = this()
  def this(
    scene: Scene,
    pluginManager: typingsJapgolly.phaser.SpinePlugin,
    x: Double,
    y: Double,
    key: String,
    animationName: Unit,
    loop: Boolean
  ) = this()
  def this(
    scene: Scene,
    pluginManager: typingsJapgolly.phaser.SpinePlugin,
    x: Double,
    y: Double,
    key: Unit,
    animationName: String,
    loop: Boolean
  ) = this()
  def this(
    scene: Scene,
    pluginManager: typingsJapgolly.phaser.SpinePlugin,
    x: Double,
    y: Double,
    key: Unit,
    animationName: Unit,
    loop: Boolean
  ) = this()
}
