package typingsJapgolly.jsforce.mod

import typingsJapgolly.jsforce.bulkMod.BulkOptions
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

@JSImport("jsforce", "Job")
@js.native
open class Job protected ()
  extends typingsJapgolly.jsforce.jobMod.Job {
  def this(bulk: typingsJapgolly.jsforce.bulkMod.Bulk) = this()
  def this(bulk: typingsJapgolly.jsforce.bulkMod.Bulk, `type`: String) = this()
  def this(bulk: typingsJapgolly.jsforce.bulkMod.Bulk, `type`: String, operation: String) = this()
  def this(bulk: typingsJapgolly.jsforce.bulkMod.Bulk, `type`: Unit, operation: String) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: String,
    operation: String,
    options: BulkOptions
  ) = this()
  def this(bulk: typingsJapgolly.jsforce.bulkMod.Bulk, `type`: String, operation: Unit, options: BulkOptions) = this()
  def this(bulk: typingsJapgolly.jsforce.bulkMod.Bulk, `type`: Unit, operation: String, options: BulkOptions) = this()
  def this(bulk: typingsJapgolly.jsforce.bulkMod.Bulk, `type`: Unit, operation: Unit, options: BulkOptions) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: String,
    operation: String,
    options: Unit,
    jobId: String
  ) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: String,
    operation: String,
    options: BulkOptions,
    jobId: String
  ) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: String,
    operation: Unit,
    options: Unit,
    jobId: String
  ) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: String,
    operation: Unit,
    options: BulkOptions,
    jobId: String
  ) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: Unit,
    operation: String,
    options: Unit,
    jobId: String
  ) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: Unit,
    operation: String,
    options: BulkOptions,
    jobId: String
  ) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: Unit,
    operation: Unit,
    options: Unit,
    jobId: String
  ) = this()
  def this(
    bulk: typingsJapgolly.jsforce.bulkMod.Bulk,
    `type`: Unit,
    operation: Unit,
    options: BulkOptions,
    jobId: String
  ) = this()
}
